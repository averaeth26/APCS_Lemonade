import java.lang.Math;
public class Person {
    double pricePoint = Math.random();
    int minIce = (int)(Math.random()*11) + (int)();
    double minSugar = (int)(Math.random()*4);
    int numLemons = (int)(Math.random()*5);
}
